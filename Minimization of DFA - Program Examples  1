class DFA:
    def __init__(self):
        self.start_state = 'A'
        self.accept_states = {'DE'}
        
        # Transition function mapping: state -> {input: next_state}
        self.transitions = {
            'A':  {'0': 'B',  '1': 'C'},
            'B':  {'0': 'C',  '1': 'DE'},
            'C':  {'0': 'C',  '1': 'DE'},
            'DE': {'0': 'DE', '1': 'DE'},
            'F':  {'0': 'F',  '1': 'DE'},
        }

    def accepts(self, input_string: str) -> bool:
        current_state = self.start_state

        for char in input_string:
            if char not in ('0', '1'):
                return False  # Rejects strings with characters outside {0, 1}
            
            current_state = self.transitions[current_state][char]

        return current_state in self.accept_states


# --- Interactive Terminal Simulator ---
if __name__ == "__main__":
    automaton = DFA()
    print("==========================================")
    print("       DFA Simulator ({0, 1})             ")
    print("==========================================")
    print("Type any string of 0s and 1s to test.")
    print("Type 'exit' to quit.\n")

    while True:
        user_input = input("Enter binary string: ").strip()
        
        if user_input.lower() == 'exit':
            print("Exiting simulator.")
            break

        if automaton.accepts(user_input):
            print("--> Result: ACCEPTED ✅\n")
        else:
            print("--> Result: REJECTED ❌\n")
